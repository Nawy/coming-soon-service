# Coming Soon Service - Environment Variables
# Copy this file to .env and fill in your values

# SECRET_TOKEN (REQUIRED)
# A secure random token used to protect the GET /coming-soon endpoint
# Generate a secure token using:
#   - Linux/Mac: openssl rand -hex 32
#   - PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
SECRET_TOKEN=your-super-secret-token-here

# EMAIL_FILE_PATH (OPTIONAL)
# Path where email addresses will be stored
# Default: emails.txt (in the application directory)
# For Docker: /app/data/emails.txt (mounted volume)
EMAIL_FILE_PATH=/app/data/emails.txt
